name: Android CI - Tests y Cobertura JaCoCo

on:
  pull_request:
    branches:
      - main
      - develop
      - 'feature/**'

jobs:
  test_and_coverage:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
      
      # 1. Configuración de Java
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'gradle'

      # 2. Permisos de Ejecución para Gradle
      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      # 3. 🚀 Ejecuta Tests y VALIDA la Cobertura (80%)
      - name: Run Unit Tests and Validate Coverage (80%)
        # Esta tarea genera el archivo .exec y el reporte XML (necesario para el paso 5)
        run: ./gradlew jacocoTestCoverageVerification

      # 4. Genera el Reporte JaCoCo HTML Report
      # NOTA: Asegúrate de que tu configuración de Gradle también genere el archivo .xml
      # La tarea jacocoTestReport debería generarlo por defecto junto al .html
      - name: Generate JaCoCo HTML/XML Report
        run: ./gradlew jacocoTestReport

      # 5. Publica la Cobertura usando una alternativa (simple-elf)
      - name: Publish Code Coverage Summary (Alternative)
        uses: simple-elf/all-in-one-coverage@v3 # ⬅️ LA ACCIÓN ALTERNATIVA
        with:
          # Ruta al archivo XML de JaCoCo que Gradle genera.
          # Esta ruta es estándar después de ejecutar jacocoTestReport.
          jacoco-coverage-path: app/build/reports/jacoco/jacocoTestReport/jacocoTestReport.xml
          # La acción automáticamente publica el resultado en el PR.
          
      # 6. Publica el reporte de tests (JUnit XML/HTML)
      - name: Publish JUnit Test Report
        uses: mikepenz/action-junit-report@v4
        if: always() 
        with:
          report_paths: '**/build/test-results/testDebugUnitTest/*.xml'